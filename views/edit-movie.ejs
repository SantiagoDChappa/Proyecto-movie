<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head.ejs') %>
    <link rel='stylesheet' href='/stylesheets/edit-movie.css'/>
    <title>Edicion: <%= movie.title %></title>
</head>
<body>
    <header>
        <a href="/"><h1>WikiPelis</h1></a>
    </header>
    <form action="/movies/edit/<%= movie.id %>" method="POST">
        <label for="title">Titulo:</label>
        <input type="text" id="title" name="title" value="<%= movie.title %>">
        <br>
        <label for="rating">Rating:</label>
        <input type="number" id="rating" name="rating" value="<%= movie.rating %>" min="1" max="10" step="0.1">
        <br>
        <label for="awards">Premios:</label>
        <input type="text" id="awards" name="awards" value="<%= movie.awards %>">
        <br>
        <label for="releaseDate">Fecha de estreno:</label>
        <input type="date" id="releaseDate" name="releaseDate" value="<%= movie.release_date %>">
        <br>
        <label for="length">Duracion:</label>
        <input type="number" id="length" name="length" value="<%= movie.length %>">
        <br>
        <label for="genres">Genero:</label>
        <select id="genres" name="genres">
            <% genres.forEach(genre => { %>
                <option value="<%= genre.id %>" <% if (movie.genre_id == genre.id) { %><%= 'selected' %><% }; %>><%= genre.name %></option>
            <% }); %>
        </select>
        <br>
        <button type="submit">ACTUALIZAR</button>
        <button type="reset">RESETEAR</button>
    </form>
</body>
</html>